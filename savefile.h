#ifndef _SAVEFILE_H
#define _SAVEFILE_H
#include <gb/gb.h>

#if 1
// acces functions must reside outside?
#define SAVECHECK_VALUE (12345)
extern uint16_t saved_check_flag;
extern uint16_t top_score_save;
#endif

#endif
